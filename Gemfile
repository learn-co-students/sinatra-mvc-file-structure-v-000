# This holds a list of all the gems needed to run the application.
# The bundler gem provides us access to a terminal command: bundle install.
#Bundler will look in the Gemfile and install any gems, as well as any gem
# dependencies for this application.

source 'http://rubygems.org'

gem 'sinatra'
gem 'tux'
gem 'require_all'
gem 'rake'
gem 'thin'
gem 'shotgun'
gem 'pry'

group :test do
  gem 'rspec'
  gem 'rack-test'
end
